#include <cstdio>

int main(){
    printf("bonjour");
    return 0;
}

//en tentant de lancer le fichier un message signale que le fichier ne s'affiche pas dans l'editeur de texte car il est soit binaire, soit dans un encodage de texte non pris en charge
//j'utilise clang++